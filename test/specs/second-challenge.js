/*
//
describe("second challenge", () => {
  before(() => {
    browser.url("./");
  });

  // The "An interactive learning project from Thinkster.
  // Code & design licensed under MIT." text in the page footer
  it("creating selector for footer ", () => {
    const thinkster = $(".container .attribution");
    expect(thinkster).toHaveTextContaining("Thinkster");
  });

  // All "Tags" in the "Popular Tags" sidebar
  it("creating selector for All 'Tags' in the 'Popular Tags' sidebar", () => {
    const popularTages = $(".sidebar p");
    expect(popularTages).toHaveTextContaining("Popular Tags");
  });

  // All "Tags" in the "Popular Tags" sidebar (using "Popular Tags" header text).
  // Hint: use `text()` and `following-sibling`
  it("creating selector for All 'Tags' in the 'Popular Tags' sidebar using `text()` and `following-sibling`", () => {
    const popularTages = $("//p[contains(text(),'Popular Tags')] ");
    expect(popularTages).toHaveTextContaining("Popular Tags");
  });
});
*/
